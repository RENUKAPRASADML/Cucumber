pipeline {
    agent any
 
    stage('Build and Test') {
            steps {
                // Run the shell script to execute Cucumber runner
                sh '''
                    #!/bin/bash
                    mvn clean test -Dtest=RunnerClassName
                '''
            }
        }
 }

